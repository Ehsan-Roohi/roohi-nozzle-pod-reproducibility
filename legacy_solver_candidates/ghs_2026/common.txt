      PARAMETER (MNM=2000000,MNC=5000,MNSC=20000,MNSP=1,MNSG=1,MNSE=100)
      DOUBLE PRECISION COL(MNSP,MNSP),MOVT,NCOL,SELT,SEPT,CS(7,MNC,MNSP)
      DOUBLE PRECISION CSR(MNC,MNSP)
      DOUBLE PRECISION CSS(9,MNSE,MNSP)
      COMMON /MOLS2 / NM,PP(2,MNM),PV(3,MNM),IPL(MNM),IPS(MNM),IR(MNM)
     &	,IRC(2,MNM)
      COMMON /MOLSR / PR(MNM)
      COMMON /CELL2 / CC(MNC),CG(7,MNC),IC(2,MNC,MNSG),ISC(MNSC),
     & CCG(2,MNC,MNSG,MNSG),ISCG(2,MNSC,MNSG),IG(2,MNSG),
     & NCX,NCY,IFCX,IFCY,CWRX,CWRY,APX,RPX,APY,RPY
      COMMON /GAS   / SP(5,MNSP),SPM(6,MNSP,MNSP),ISP(MNSP)
      COMMON /GASR  / SPR(3,MNSP,MNSP),ISPR(3,MNSP),CT(MNC)
      COMMON /SAMP2 / COL,NCOL,MOVT,SELT,SEPT,CS,TIME,NPR,NSMP,FND,FTMP,
     &TIMI,FSP(MNSP),ISPD,VFX,VFY
      COMMON /SAMPR / CSR,Tsm(MNSE),Vx(MNSE)

      COMMON /SAMPS / CSS,NSM,iter,QQs(MNSE),TTs(MNSE),Qsm(MNSE)
    
      COMMON /COMP  / FNUM,DTM,NIS,NSP,NPS,NPT,QwEPS,QwRF,ITypeQw,Twb
	
      COMMON /GEOM2 / NSCX,NSCY,CB(10),IB(6),ISURF(3),LIMS(2,3),IIS,ISG,
     & TSURF(3),Tw(110),IJET,LIMJ(3),TMPJ,FNDJ,FVJ,FSPJ(MNSP),NSQ,
     & BMEJ(MNSP),BMRJ(MNSP),BME(6,MNSP),BMR(6,MNSP),CW,DesQw(MNSE),
     & FW,CH,FH,WJ,LFLX,LFLY,ALPI(3),ALPN(2),ALPT(2),CollRate(MNSE),
     & XGRID(101,61),YGRID(101,61),INZ(3),XYN(3,2),X_B(41,51),Y_B(41,51)
      COMMON /CONST / PI,SPI,BOLTZ
      COMMON /HIST / RM0,RML,U0,UL
      REAL::BMEinlet(100),BMEoutlet(100),BMElower(100)
      COMMON /BMEDATA/BMEinlet,BMEoutlet,BMElower
	  INTEGER IPROB(MNC)
	  COMMON /MESHDATA/ IPROB,IPLOT

	INTEGER::NBX,NBY
	REAL::BF(4),H_BUFFER
	COMMON /BUFFER /BF,H_BUFFER,NBX,NBY

C     GHS molecular model parameters
C     IMOLMODEL = 1: original VHS/VSS formulation; IMOLMODEL = 2: GHS
      INTEGER IMOLMODEL
      DOUBLE PRECISION GHS_SIG,GHS_EPSK,GHS_A1,GHS_A2,GHS_W1,GHS_W2
      COMMON /GHSPAR/ GHS_SIG,GHS_EPSK,GHS_A1,GHS_A2,GHS_W1,
     &                GHS_W2,IMOLMODEL

